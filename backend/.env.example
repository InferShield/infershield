# InferShield Backend Configuration

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/infershield

# Authentication
JWT_SECRET=your-secret-key-here-min-32-chars

# Server
PORT=5000
NODE_ENV=development

# Stripe (for billing)
STRIPE_SECRET_KEY=sk_test_your_stripe_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# ⚠️ PASSTHROUGH MODEL - NO UPSTREAM API KEYS
# InferShield uses a passthrough model: developers send their own LLM API keys
# in the Authorization header of each request. InferShield NEVER stores or logs
# these keys. DO NOT set OPENAI_API_KEY, ANTHROPIC_API_KEY, or any upstream
# provider keys in this file or any other configuration.
#
# Correct architecture:
#   Developer App → InferShield (X-InferShield-Key + Authorization: Bearer sk-openai-...)
#   → InferShield validates, scans, forwards to OpenAI with developer's key
#   → OpenAI responds → InferShield logs (without key) → Developer App
#
# OPENAI_API_KEY=  # DO NOT SET - passthrough model only
# ANTHROPIC_API_KEY=  # DO NOT SET - passthrough model only
